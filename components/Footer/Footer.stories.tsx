import type { Meta } from '@storybook/react';

import { Footer } from './Footer';

const meta = {
  title: 'Designsystem / Page Components / Footer',
  component: Footer,
} satisfies Meta<typeof Footer>;

export default meta;

export const Default = {};

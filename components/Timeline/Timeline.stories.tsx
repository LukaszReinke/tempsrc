import type { Meta } from '@storybook/react';

import { Timeline } from './Timeline';

const meta = {
  title: 'Designsystem / Page Components / Timeline',
  component: Timeline,
} satisfies Meta<typeof Timeline>;

export default meta;

export const Default = {};

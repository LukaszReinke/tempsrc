import type { Meta } from '@storybook/react';

import { UrlInput } from './UrlInput';

const meta = {
  title: 'Designsystem / Page Components / URL Input',
  component: UrlInput,
} satisfies Meta<typeof UrlInput>;

export default meta;

export const Default = {};

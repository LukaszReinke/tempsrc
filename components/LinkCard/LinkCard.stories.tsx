import type { Meta } from '@storybook/react';

import { LinkCard } from './LinkCard';

const meta = {
  title: 'Designsystem / UI Atoms / Link Card',
  component: LinkCard,
} satisfies Meta<typeof LinkCard>;

export default meta;

export const Default = {};

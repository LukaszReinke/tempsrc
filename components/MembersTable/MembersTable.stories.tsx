import type { Meta } from '@storybook/react';

import { MembersTable } from './MembersTable';

const meta = {
  title: 'Designsystem / Page Components / MembersTable',
  component: MembersTable,
} satisfies Meta<typeof MembersTable>;

export default meta;

export const Default = {};

import { Meta } from '@storybook/react';
import { ContentLoader } from './ContentLoader';

export default {
  title: 'Designsystem / UI Atoms / Loaders / Content Loader',
  component: ContentLoader,
} as Meta<typeof ContentLoader>;

export const Default = {};

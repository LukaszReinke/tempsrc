import { Meta } from '@storybook/react';
import { Spinner } from './Spinner';

export default {
  title: 'Designsystem / UI Atoms / Loaders / Spinner',
  component: Spinner,
} as Meta<typeof Spinner>;

export const Default = {};

import { Meta } from '@storybook/react';
import { SimpleLoader } from './SimpleLoader';

export default {
  title: 'Designsystem / UI Atoms / Loaders / Simple Loader',
  component: SimpleLoader,
} as Meta<typeof SimpleLoader>;

export const Default = {};
